# 📖 Python Dictionary

A personal Python dictionary containing simple definitions of every concept I've learned on my journey to becoming an AI Engineer.

---

# 📅 Day 1 – Python Basics

## Python
A programming language used to build software, websites, automate tasks, and create AI.

## `print()`
Displays output on the screen.

## Variable
A named container used to store data.

## String (`str`)
Text enclosed in quotation marks.

## Comment (`#`)
A note in the code that Python ignores.

---

# 📅 Day 2 – Git & GitHub

## Git
A version control system that tracks changes in your code.

## GitHub
A website for storing and sharing Git repositories.

## Repository (Repo)
A folder where your project's code and history are stored.

## Commit
A saved snapshot of your project.

## Push
Uploads your local commits to GitHub.

## Clone
Downloads a repository from GitHub to your computer.

---

# 📅 Day 3 – Variables & Input

## Variable
Stores a value.

## `input()`
Gets information from the user.

## Assignment (`=`)
Assigns a value to a variable.

## Data Type
The kind of value stored in a variable, such as a string or integer.

---

# 📅 Day 4 – If & Else

## `if`
Runs code only if a condition is true.

## `else`
Runs when the `if` condition is false.

## Condition
An expression that evaluates to either `True` or `False`.

## Comparison (`==`)
Checks if two values are equal.

## `!=`
Checks if two values are different.

## `>`
Checks if one value is greater than another.

## `<`
Checks if one value is less than another.

---

# 📅 Day 5 – `elif`

## `elif`
Checks another condition if the previous condition was false.

## Decision Making
Choosing which code to run based on conditions.

## `if` Chain
Stops checking conditions after the first matching condition.

## Separate `if` Statements
Checks every condition independently.

---

# 📅 Day 6 – While Loops

## `while` Loop
Repeats code while a condition is true.

## Infinite Loop
A loop that never stops because its condition never becomes false.

## Iteration
One complete repetition of a loop.

---

# 📅 Day 7 – Functions

## Function
A reusable block of code that performs a specific task.

## `def`
Defines a function.

## Function Call
Runs a function.

---

# 📅 Day 8 – Parameters

## Parameter
A variable inside a function that receives a value.

## Argument
The actual value passed to a function.

---

# 📅 Day 9 – Return Values

## `return`
Sends a value back from a function.

## Return Value
The value sent back by a function.

---

# 📅 Day 10 – Lists

## List
A collection of multiple items stored in one variable.

## Index
The position of an item in a list. Indexes start at **0**.

## `len()`
Returns the number of items in a list.

---

# 📅 Day 11 – Modifying Lists

## `.append()`
Adds an item to the end of a list.

## `.remove()`
Removes an item from a list by its value.

## Replace an Item
Changes an item using its index.

```python
fruits[1] = "Mango"
```

---

# 📅 Day 12 – `for` Loops

## `for` Loop
Repeats an action for every item in a collection.

## Loop Variable
A temporary variable that stores the current item during each iteration.

## Collection
A group of items, such as a list.

---

# 📅 Day 13 – `range()`

## `range()`
Creates a sequence of numbers.

## Start Value
The first number in the sequence.

## Stop Value
The number where the sequence stops (the stop value is **not included**).

## Step Value
The amount by which the numbers increase or decrease each time.

---

# 🧠 Quick Reference

| Concept | Simple Definition |
|---------|-------------------|
| Variable | Stores a value. |
| `print()` | Displays output on the screen. |
| `input()` | Gets input from the user. |
| `=` | Assigns a value. |
| `==` | Compares two values. |
| `if` | Runs code if a condition is true. |
| `elif` | Checks another condition if needed. |
| `else` | Runs when no previous condition is true. |
| `while` | Repeats code while a condition is true. |
| Function | A reusable block of code. |
| Parameter | A variable inside a function. |
| Argument | The value passed to a function. |
| `return` | Sends a value back from a function. |
| List | Stores multiple items in one variable. |
| Index | The position of an item in a list. |
| `.append()` | Adds an item to a list. |
| `.remove()` | Removes an item from a list. |
| `for` | Repeats an action for every item. |
| `range()` | Creates a sequence of numbers. |

---

---

# 📅 Day 14 – Strings

## String
A sequence of characters enclosed in quotation marks.

## Character
A single letter, number, symbol, or space in a string.

## Index
The position of a character in a string or an item in a list. Indexes start at **0**.

## Positive Index
Counts characters from the beginning of a string.

Example:

```python
name = "Peter"

print(name[0])
```

Output:

```text
P
```

## Negative Index
Counts characters from the end of a string.

Example:

```python
name = "Peter"

print(name[-1])
```

Output:

```text
r
```

## `len()`
Returns the number of characters in a string or the number of items in a collection.

## `.upper()`
Converts all letters in a string to uppercase.

Example:

```python
name = "Peter"

print(name.upper())
```

Output:

```text
PETER
```

## `.lower()`
Converts all letters in a string to lowercase.

Example:

```python
name = "Peter"

print(name.lower())
```

Output:

```text
peter
```

## `.title()`
Capitalizes the first letter of each word in a string.

Example:

```python
name = "peter audu"

print(name.title())
```

Output:

```text
Peter Audu
```

## `.strip()`
Removes spaces from the beginning and end of a string.

Example:

```python
name = "   Peter   "

print(name.strip())
```

Output:

```text
Peter
```

## `in`
Checks whether a character or word exists inside a string.

Example:

```python
name = "Peter"

print("e" in name)
```

Output:

```text
True
```

---

# 💡 Day 14 Key Takeaways

- Strings are sequences of characters.
- Every character has an index.
- Positive indexes count from the beginning.
- Negative indexes count from the end.
- `len()` counts characters in a string.
- `.upper()` converts text to uppercase.
- `.lower()` converts text to lowercase.
- `.title()` capitalizes the first letter of each word.
- `.strip()` removes unwanted spaces from the beginning and end of a string.
- `in` checks whether text exists inside another string.

---

# 🚀 Journey Progress

- ✅ Day 1 – Python Basics
- ✅ Day 2 – Git & GitHub
- ✅ Day 3 – Variables & Input
- ✅ Day 4 – If & Else
- ✅ Day 5 – `elif`
- ✅ Day 6 – While Loops
- ✅ Day 7 – Functions
- ✅ Day 8 – Parameters
- ✅ Day 9 – Return Values
- ✅ Day 10 – Lists
- ✅ Day 11 – Modifying Lists
- ✅ Day 12 – `for` Loops
- ✅ Day 13 – `range()`
- ✅ Day 14 - Strings

---

---

# 📅 Day 15 – Dictionaries

## Dictionary
A collection that stores data as **key-value pairs**.

Example:

```python
student = {
    "name": "Peter",
    "age": 24
}
```

---

## Key
A unique identifier used to access a value in a dictionary.

Example:

```python
student["name"]
```

Here:

```text
"name"
```

is the key.

---

## Value
The information stored for a particular key.

Example:

```python
student = {
    "name": "Peter"
}
```

Here:

```text
Peter
```

is the value.

---

## Accessing a Value

Use the key inside square brackets.

Example:

```python
student = {
    "name": "Peter"
}

print(student["name"])
```

Output:

```text
Peter
```

---

## Adding Data

Add a new key-value pair.

Example:

```python
student["course"] = "Mathematics"
```

---

## Updating Data

Change the value of an existing key.

Example:

```python
student["age"] = 25
```

---

## Deleting Data

Use the `del` keyword.

Example:

```python
del student["age"]
```

---

## Looping Through a Dictionary

Loop through the keys.

Example:

```python
for key in student:
    print(key)
```

Loop through the values.

Example:

```python
for key in student:
    print(student[key])
```

---

# 💡 Day 15 Key Takeaways

- Dictionaries store data as key-value pairs.
- Keys are used to access values.
- Every key should be unique.
- Dictionaries make related information easier to organize.
- Values can be added, updated, or deleted.
- Dictionaries can be looped through using a `for` loop.

---

## 🧠 Memory Sentences

Dictionary → Stores data as key-value pairs.

Key → A unique identifier used to access a value.

Value → The information stored for a key.

`dictionary[key]` → Accesses the value for a specific key.

`del` → Removes a key-value pair from a dictionary.

`for key in dictionary:` → Loops through every key in a dictionary.

---

---

# 📅 Day 16 – Tuples

## Tuple

A collection used to store multiple items. Tuples are ordered and immutable.

Example:

```python
fruits = ("Apple", "Banana", "Orange")
```

---

## Immutable

A value that cannot be changed after it has been created.

Example:

```python
numbers = (1, 2, 3)
```

This is **not allowed**:

```python
numbers[0] = 10
```

---

## Tuple Indexing

Access items using their position.

Example:

```python
fruits = ("Apple", "Banana", "Orange")

print(fruits[0])
```

Output:

```text
Apple
```

---

## Negative Indexing

Access items from the end.

Example:

```python
print(fruits[-1])
```

Output:

```text
Orange
```

---

## `len()`

Returns the number of items in a tuple.

Example:

```python
numbers = (10, 20, 30)

print(len(numbers))
```

Output:

```text
3
```

---

## Looping Through a Tuple

Example:

```python
for fruit in fruits:
    print(fruit)
```

---

# 💡 Day 16 Key Takeaways

- Tuples use parentheses `()`.
- Tuples are immutable.
- Tuple indexing works the same as list indexing.
- `len()` counts the number of items in a tuple.
- `for` loops can iterate through tuples.
- Tuples are useful for data that should not change.

---

## 🧠 Memory Sentences

Tuple → An ordered, immutable collection.

Immutable → Cannot be changed after creation.

Tuple indexing → Works just like list indexing.

`len()` → Counts the number of items.

`for item in tuple:` → Loops through every item.

---
---

# 📅 Day 17 – Sets

## Set

A collection that stores unique items. Duplicate values are automatically removed.

Example:

```python
fruits = {"Apple", "Banana", "Orange"}
```

---

## Unique Items

A set keeps only one copy of each value.

Example:

```python
numbers = {1, 2, 2, 3, 3, 3}

print(numbers)
```

Output:

```text
{1, 2, 3}
```

---

## `.add()`

Adds a new item to a set.

Example:

```python
fruits.add("Mango")
```

---

## `.remove()`

Removes an item from a set.

Example:

```python
fruits.remove("Banana")
```

---

## `len()`

Returns the number of unique items in a set.

Example:

```python
len(fruits)
```

---

## Loop Through a Set

Example:

```python
for fruit in fruits:
    print(fruit)
```

---

## Set vs Dictionary

### Set

Stores only values.

```python
{"Apple", "Banana", "Orange"}
```

### Dictionary

Stores key-value pairs.

```python
{
    "name": "Peter",
    "age": 24
}
```

---

# 💡 Day 17 Key Takeaways

- Sets use curly braces `{}`.
- Sets store only unique items.
- Duplicate values are ignored.
- Use `.add()` to add items.
- Use `.remove()` to remove items.
- `len()` counts unique items.
- `for` loops work with sets.
- Sets do not store key-value pairs.

---

## 🧠 Memory Sentences

Set → A collection of unique values.

Duplicate → A repeated value.

`.add()` → Adds an item.

`.remove()` → Removes an item.

Sets automatically ignore duplicates.

---

---

# 📅 Day 18 – Modules

## Module

A Python file that contains reusable code such as functions, variables, or classes.

Example:

```python
# calculator.py

def add(a, b):
    return a + b
```

---

## Import a Module

Imports everything from a module.

Example:

```python
import calculator

print(calculator.add(5, 3))
```

---

## Import a Specific Function

Imports only the function you need.

Example:

```python
from calculator import add

print(add(5, 3))
```

---

## Built-in Modules

Python comes with many built-in modules.

### `math`

Provides mathematical functions.

Example:

```python
import math

print(math.sqrt(25))
```

Output:

```text
5.0
```

---

### `random`

Generates random values.

Example:

```python
import random

print(random.randint(1, 10))
```

---

# 💡 Day 18 Key Takeaways

- A module is a reusable Python file.
- Modules help organize large programs.
- `import module` imports the entire module.
- `from module import function` imports only the specified function.
- Python includes many useful built-in modules.

---

## 🧠 Memory Sentences

Module → A reusable Python file.

`import module` → Import the whole module.

`from module import function` → Import only one function.

`math` → Mathematical operations.

`random` → Generates random values.

---

# 📅 Day 19 – File Handling

## File Handling

The process of creating, reading, writing, and updating files using Python.

Example:

```python
file = open("notes.txt", "w")
```

---

## `open()`

Opens a file.

Example:

```python
file = open("notes.txt", "r")
```

---

## Write Mode (`"w"`)

Creates a new file if it doesn't exist.

If the file already exists, its contents are replaced.

Example:

```python
file = open("notes.txt", "w")
file.write("Hello")
```

---

## Read Mode (`"r"`)

Reads the contents of a file.

Example:

```python
file = open("notes.txt", "r")
print(file.read())
```

---

## Append Mode (`"a"`)

Adds new content to the end of an existing file without deleting the previous content.

Example:

```python
file = open("notes.txt", "a")
file.write("\nWelcome back!")
```

---

## `.write()`

Writes data into a file.

Example:

```python
file.write("Python is awesome!")
```

---

## `.read()`

Reads everything inside a file.

Example:

```python
print(file.read())
```

---

## `.close()`

Closes a file after you finish using it.

Example:

```python
file.close()
```

---

# 💡 Day 19 Key Takeaways

- File handling lets programs save information permanently.
- `open()` opens a file.
- `"w"` writes and replaces existing content.
- `"r"` reads a file.
- `"a"` adds new content without deleting existing content.
- Always close files after using them.

---

## 🧠 Memory Sentences

File Handling → Saving and reading information from files.

`open()` → Opens a file.

`"w"` → Write (creates or replaces).

`"r"` → Read.

`"a"` → Append (adds to the end).

`.write()` → Writes data.

`.read()` → Reads data.

`.close()` → Closes the file.

---

> **"Programming isn't about memorising syntax. It's about understanding ideas and solving problems one step at a time."**

**Current Streak:** 🔥 Day 19 / 365